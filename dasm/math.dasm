func S 0 {
  star {
    define read c;
    if ne c ' ' {
      if ne c '\t' {
        if ne c '\r' {
          if ne c '\n' {
            fail;
          }
        }
      }
    }
  }
}

func Digits 0 {
  plus {
    define read c;
    if lt c '0' {
      fail;
    }
    if gt c '9' {
      fail;
    }
  }
}

func Integer 1 string {
  define slice {
    call Digits;
  } v;
  call S;
  return 1 v;
}
